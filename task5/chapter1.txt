1   The operators are +, -, *, and /. 
    The values are 'hello', -88.8, and 5.

2   The string is 'spam' because strings always starts with quote

3   integers, floating-point numbers, and strings.

4   expression is a combination of values and operators. All expressions evaluate to a single value.

5   expression evaluates to a single value while A statement will not.

6  The bacon variable is 20. The bacon + 1 expression does not reassign the value in bacon'

7   'spamspamspam'.

8  Variable names cannot begin with a number.

9  The int(), float(), and str() functions will evaluate to the integer, floating-point number, and string versions of the value passed to them.

10  The expression causes an error because 99 is an integer,
     and only strings can be concatenated to other strings with the + operator. 
     The correct way is I have eaten ' + str(99) + ' burritos.'.